# Atrium client

Owns authentication-neutral request/response types, the `AtriumGateway` interface, OpenAPI-generated TypeScript models when available, retry classification, idempotency headers, and test doubles.

It must not depend on Chrome APIs or macOS frameworks.
