# Capture core

Owns normalized action semantics, event merging, deterministic step ordering, instruction fallbacks, session state transitions, and schema migrations.

DOM selectors and Accessibility objects are inputs from platform adapters, not dependencies of this package.

