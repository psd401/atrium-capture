# Synthetic fixture site

Serve this directory on `http://127.0.0.1:4173` for browser integration tests. Every name and value is invented. The password control exists only to prove that capture code never reads or retains password values.
